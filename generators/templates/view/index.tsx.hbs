
import { ReactNode } from "react";

import { Flex } from "@chakra-ui/react";

export type {{pascalCase name}}PageProps = {
  children?: ReactNode
}

const {{pascalCase name}} = ({ children }: {{pascalCase name}}PageProps) => (
  <Flex>
    {children}
  </Flex>
);

export { {{pascalCase name}} };
